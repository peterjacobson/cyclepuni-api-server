<%= form_for([@bike, @loan]) do |f| %>
  <% if @loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% @loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_due %><br>
    <%= f.date_select :date_due %>
  </div>
  <div class="field">
    <%= f.label :contribution_status %><br>
    <%= f.text_field :contribution_status %>
  </div>
  <div class="field">
    <%= f.label :contribution_notes %><br>
    <%= f.text_field :contribution_notes %>
  </div>
  <div class="field">
    <%= f.label :loan_status %><br>
    <%= f.text_field :loan_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
